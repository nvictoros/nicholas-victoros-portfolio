export type Link = {
    id: number,
    label: string,
    icon: JSX.Element,
    url: string
  }
  
export type Project = {
    id: number,
    label: string,
    description: string,
    url: string
  }